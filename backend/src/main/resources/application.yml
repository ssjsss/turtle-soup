server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/turtle_soup?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: turtlesoup
    password: Ts2024!soup
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# JWT 配置
jwt:
  secret: turtle-soup-jwt-secret-key-2024-this-is-a-long-enough-key
  expiration: 86400000  # 24 hours

# DeepSeek API 配置
llm:
  provider: deepseek
  api-key: sk-c6f080a9d6c04cd1a5fbb96706f6fd77
  model: deepseek-chat
  base-url: https://api.deepseek.com/v1
